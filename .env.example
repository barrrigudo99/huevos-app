# Origen del backend Express (sin "/api" al final, src/api.js lo añade).
# En local: http://localhost:4000 (puerto por defecto de server/index.js).
# En Vercel: la URL pública del túnel de Cloudflare que expone el backend.
VITE_API_URL=http://localhost:4000
